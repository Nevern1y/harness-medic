# Example reports

`minimal.json` is a schema-valid, fully sanitized report used by release validation. Real reports can contain local paths and source metadata; do not publish them without reviewing redaction and workspace ownership.
